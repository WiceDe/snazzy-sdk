/* eslint consistent-return: "off" */
/* eslint no-multi-str: "off" */
/* eslint-disable no-unused-expressions */

const { expect } = require('chai');

describe('Base tests', () => {
  it('should ensure that test suite is functional', async () => {
    expect(true).to.be.true;
  });
});
